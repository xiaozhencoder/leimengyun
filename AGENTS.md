# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty project (blank `README.md` only). There are no application services, dependencies, build systems, or tests to run.

- **No package manager lockfiles** — no `package.json`, `requirements.txt`, `pyproject.toml`, or similar exist.
- **No services** — no backend, frontend, database, or other services to start.
- **No lint/test/build commands** — none are configured yet.

Once application code is added, update this section with relevant startup, testing, and development caveats.
